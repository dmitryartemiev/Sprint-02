function myFunc(){
    var freedom = 'Freedom is power'
    alert (freedom)
}
myFunc()
function calcMySquare(number){
    x = number * number
    alert ('The square of ' + number + ' is ' + x )
}
calcMySquare(2)